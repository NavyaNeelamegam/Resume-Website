/* Claude-inspired Theme for Navya's Resume */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Source+Serif+Pro:wght@400;600;700&display=swap');

:root {
    /* Claude Color Palette */
    --primary-color: #8C52FF; /* Primary Claude purple */
    --primary-dark: #6B40C8; /* Darker purple for hover states */
    --primary-light: #E2D6FF; /* Light purple for backgrounds */
    --secondary-color: #3D3D48; /* Dark gray for text */
    --tertiary-color: #8F8F98; /* Medium gray for secondary text */
    --background-color: #F9F7FE; /* Very light purple for backgrounds */
    --white: #FFFFFF;
    --light-gray: #F2F2F5;
    --border-color: #E6E6EB;
    --timeline-color: #D9CBFD;
    --shadow: 0 4px 10px rgba(140, 82, 255, 0.1);
    --transition: all 0.3s ease;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: 'Inter', sans-serif;
    line-height: 1.6;
    color: var(--secondary-color);
    background-color: var(--white);
}

.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px;
}

a {
    text-decoration: none;
    color: var(--primary-color);
    transition: var(--transition);
}

a:hover {
    color: var(--primary-dark);
}

ul {
    list-style: none;
}

img {
    max-width: 100%;
    height: auto;
    display: block;
}

.section {
    padding: 80px 0;
}

.section-title {
    font-family: 'Source Serif Pro', serif;
    font-size: 2.2rem;
    color: var(--primary-color);
    margin-bottom: 40px;
    text-align: center;
    position: relative;
}

.section-title::after {
    content: '';
    display: block;
    width: 60px;
    height: 3px;
    background: var(--primary-color);
    margin: 10px auto 0;
}

/* Navigation Bar */
.navbar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    background-color: var(--white);
    box-shadow: var(--shadow);
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 5%;
    z-index: 1000;
}

.logo a {
    font-family: 'Source Serif Pro', serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--primary-color);
}

.nav-links {
    display: flex;
    gap: 25px;
}

.nav-links li a {
    font-weight: 500;
    font-size: 0.95rem;
    color: var(--secondary-color);
    transition: var(--transition);
}

.nav-links li a:hover {
    color: var(--primary-color);
}

.hamburger {
    display: none;
    cursor: pointer;
}

.bar {
    width: 25px;
    height: 3px;
    background-color: var(--primary-color);
    margin: 5px 0;
    transition: var(--transition);
}

/* Header Section */
.header {
    padding-top: 120px;
    padding-bottom: 80px;
    background-color: var(--background-color);
    background-image: linear-gradient(to right, rgba(140, 82, 255, 0.05), rgba(140, 82, 255, 0.1));
    text-align: center;
}

.header-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 30px;
}

.profile-image {
    width: 220px;
    height: 220px;
    border-radius: 50%;
    overflow: hidden;
    border: 5px solid var(--white);
    box-shadow: var(--shadow);
}

.profile-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.header-text h1 {
    font-family: 'Source Serif Pro', serif;
    font-size: 2.8rem;
    color: var(--primary-color);
    margin-bottom: 10px;
}

.header-text h2 {
    font-size: 1.3rem;
    color: var(--tertiary-color);
    font-weight: 400;
    margin-bottom: 20px;
}

.social-links {
    display: flex;
    gap: 20px;
    justify-content: center;
    margin-top: 15px;
}

.social-links a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background-color: var(--white);
    border-radius: 50%;
    color: var(--primary-color);
    font-size: 1.2rem;
    transition: var(--transition);
    box-shadow: var(--shadow);
}

.social-links a:hover {
    background-color: var(--primary-color);
    color: var(--white);
    transform: translateY(-3px);
}

/* About Section */
.about {
    background-color: var(--white);
}

.about-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
}

.about-content p {
    margin-bottom: 30px;
    font-size: 1.05rem;
    line-height: 1.8;
    color: var(--secondary-color);
}

.contact-info {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    margin: 30px 0;
}

.contact-item {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--tertiary-color);
}

.contact-item i {
    color: var(--primary-color);
    font-size: 1.1rem;
}

.languages {
    margin-top: 30px;
}

.languages h3 {
    font-size: 1.2rem;
    color: var(--primary-color);
    margin-bottom: 15px;
    font-family: 'Source Serif Pro', serif;
}

.language-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 15px;
}

.language-list li {
    background-color: var(--primary-light);
    padding: 8px 15px;
    border-radius: 20px;
    font-size: 0.9rem;
    color: var(--secondary-color);
}

.language-list li span {
    font-weight: 600;
    color: var(--primary-color);
}

/* Education Section */
.education {
    background-color: var(--background-color);
}

.timeline {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
}

.timeline::before {
